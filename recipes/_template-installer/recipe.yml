# Vorlage installer_offline — Kopie via: ./scripts/new-recipe.sh meine-app --type installer
id: example-app
name: "Example App (Template)"
category: "Vorlage"
version_guaranteed: "1.0.0"
version_label: "Example 1.0.0"
data_root: "~/.local/share/wine-software/example-app"
prefix: "{data_root}/prefix"
runtime: proton-ge
install_type: installer_offline
source_kind: fixed_path
installer_dir: "{repo}/installer"
source_label: "Offline-Installer (Ordner mit setup.exe)"
fix_kind: none
exe_glob: "**/*.exe"
winetricks: [win10, vcrun2015]
env:
  WINEDEBUG: "-all"
validate: validate.sh
launch: launch.sh
install: install.sh
repair: repair.sh
kill: kill.sh
uninstall: uninstall.sh
install_steps:
  - prepare_source
  - prefix
  - winetricks
  - run_installer
